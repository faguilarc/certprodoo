<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ─── Vista Lista ──────────────────────────────────────── -->
    <record id="structure_view_tree" model="ir.ui.view">
        <field name="name">certprodoo.structure.tree</field>
        <field name="model">certprodoo.structure</field>
        <field name="arch" type="xml">
            <tree string="Estructuras">
                <field name="name"/>
                <field name="description"/>
                <field name="company_id" groups="base.group_multi_company"/>
            </tree>
        </field>
    </record>

    <!-- ─── Vista Formulario ─────────────────────────────────── -->
    <record id="structure_view_form" model="ir.ui.view">
        <field name="name">certprodoo.structure.form</field>
        <field name="model">certprodoo.structure</field>
        <field name="arch" type="xml">
            <form string="Estructura">
                <header>
                    <button name="toggle_active" type="object"
                            string="Archivar" class="btn-secondary"
                            invisible="active == False"
                            groups="certprodoo_nomenclators.group_nomenclator_manager"/>
                    <button name="toggle_active" type="object"
                            string="Desarchivar" class="btn-secondary"
                            invisible="active == True"
                            groups="certprodoo_nomenclators.group_nomenclator_manager"/>
                </header>
                <sheet>
                    <field name="active" invisible="1"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="Nombre de la estructura"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="code"/>
                            <field name="description" placeholder="Descripción de la estructura"/>
                        </group>
                        <group>
                            <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ─── Acción ───────────────────────────────────────────── -->
    <record id="structure_action" model="ir.actions.act_window">
        <field name="name">Estructuras</field>
        <field name="res_model">certprodoo.structure</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Crear una nueva estructura
            </p>
            <p>
                Defina las estructuras organizativas del sistema para el portal web.
            </p>
        </field>
    </record>

</odoo>
