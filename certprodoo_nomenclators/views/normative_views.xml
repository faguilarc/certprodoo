<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ─── Vista Lista ──────────────────────────────────────── -->
    <record id="normative_view_tree" model="ir.ui.view">
        <field name="name">certprodoo.normative.tree</field>
        <field name="model">certprodoo.normative</field>
        <field name="arch" type="xml">
            <tree string="Normativas">
                <field name="name"/>
                <field name="code"/>
                <field name="company_id" groups="base.group_multi_company"/>
            </tree>
        </field>
    </record>

    <!-- ─── Vista Formulario ─────────────────────────────────── -->
    <record id="normative_view_form" model="ir.ui.view">
        <field name="name">certprodoo.normative.form</field>
        <field name="model">certprodoo.normative</field>
        <field name="arch" type="xml">
            <form string="Normativa">
                <header>
                    <button name="toggle_active" type="object"
                            string="Archivar" class="btn-secondary"
                            invisible="active == False"
                            groups="certprodoo_nomenclators.group_nomenclator_manager"/>
                    <button name="toggle_active" type="object"
                            string="Desarchivar" class="btn-secondary"
                            invisible="active == True"
                            groups="certprodoo_nomenclators.group_nomenclator_manager"/>
                </header>
                <sheet>
                    <field name="active" invisible="1"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="Nombre de la normativa"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="code"/>
                            <field name="attachment_ids" widget="many2many_binary"
                                   string="Documentos Adjuntos"/>
                        </group>
                        <group>
                            <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ─── Acción ───────────────────────────────────────────── -->
    <record id="normative_action" model="ir.actions.act_window">
        <field name="name">Normativas</field>
        <field name="res_model">certprodoo.normative</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Crear una nueva normativa
            </p>
            <p>
                Defina las normativas legales del sistema con sus documentos adjuntos.
            </p>
        </field>
    </record>

</odoo>
